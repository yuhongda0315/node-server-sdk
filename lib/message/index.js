"use strict";

const History = require('./history');

const Private = require('./private');

const Chatroom = require('./chatroom');

const Disuccsion = require('./discussion');

const System = require('./system');

const Group = require('./group');

module.exports = {
	History: History,
	Private: Private,
	Chatroom: Chatroom,
	Disuccsion: Disuccsion,
	System: System,
	Group: Group
};
