## 群组模块

### Group.sync(user){#sync}

把用户的多个群同步到融云服务器，每次同步是全量群组数据，群组关系会以最后一次同步为准

`user` 参数说明:

| 参数   	 		|	类型		| 必填	| 说明 							|最低版本	|
| :----------------	|:--------	|:-----	|:------------------------------|:----- |
| id		  		| string 	| 	是 	| 用户 Id，最大长度 30 个字符，建议使用 `英文字母`、`数字` 混排	 | 2.0.0 |
| groups	  		| array 	| 	是 	| 用户所在群组列表 | 2.0.0 |

`groups` 说明: 

```json
[{
	"id": "mdk91sdj",
	"name": "RongCloud"
}]
```

| 参数   	 		|	类型		| 必填	| 说明 							|最低版本	|
| :----------------	|:--------	|:-----	|:------------------------------|:----- |
| id		  		| string 	| 	是 	| 群组 Id，最大长度 30 个字符，建议使用 `英文字母`、`数字` 混排	 | 2.0.0 |
| name	  			| stirng 	| 	是 	| 群组名称，最大长度 60 个字符 | 2.0.0 |

##### 请求成功

```json
{
    "code": 200
}
```

### Group.create(group){#create}

创建群组，每个用户最多可加入 500 个群，每个群最多 3000 人，应用内群组数量无限制

`group` 参数说明:

| 参数   	 		|	类型		| 必填	| 说明 							|最低版本	|
| :----------------	|:--------	|:-----	|:------------------------------|:----- |
| id		  		| string 	| 	是 	| 群组 Id，最大长度 30 个字符，建议使用 `英文字母`、`数字` 混排	 | 2.0.0 |
| name	  			| string 	| 	是 	| 用户所在群组，最大长度 60 个字符 | 2.0.0 |
| memberIds	  		| array 	| 	是 	| 群成员 Id 列表 | 2.0.0 |

##### 请求成功

```json
{
    "code": 200
}
```

### Group.invite(group){#invite}

邀请人入群组

`group` 参数说明:

| 参数   	 		|	类型		| 必填	| 说明 							|最低版本	|
| :----------------	|:--------	|:-----	|:------------------------------|:----- |
| id		  		| string 	| 	是 	| 群组 Id，最大长度 30 个字符，建议使用 `英文字母`、`数字` 混排	 | 2.0.0 |
| memberIds	  		| array 	| 	是 	| 邀请入群的成员 Id 列表 | 2.0.0 |

##### 请求成功

```json
{
    "code": 200
}
```

### Group.join(group){#join}

加入群组

`group` 参数说明:

| 参数   	 		|	类型		| 必填	| 说明 							|最低版本	|
| :----------------	|:--------	|:-----	|:------------------------------|:----- |
| id		  		| string 	| 	是 	| 群组 Id，最大长度 30 个字符，建议使用 `英文字母`、`数字` 混排	 | 2.0.0 |
| memberId	  		| string 	| 	是 	| 加群成员 Id | 2.0.0 |

##### 请求成功

```json
{
    "code": 200
}
```

### Group.quit(group){#quit}

退出群组

`group` 参数说明:

| 参数   	 		|	类型		| 必填	| 说明 							|最低版本	|
| :----------------	|:--------	|:-----	|:------------------------------|:----- |
| id		  		| string 	| 	是 	| 群组 Id，最大长度 30 个字符，建议使用 `英文字母`、`数字` 混排	 | 2.0.0 |
| memberId	  		| string 	| 	是 	| 退出群成员 Id | 2.0.0 |

##### 请求成功

```json
{
    "code": 200
}
```

### Group.kick(group){#kick}

将群成员移除群组

`group` 参数说明:

| 参数   	 		|	类型		| 必填	| 说明 							|最低版本	|
| :----------------	|:--------	|:-----	|:------------------------------|:----- |
| id		  		| string 	| 	是 	| 群组 Id，最大长度 30 个字符，建议使用 `英文字母`、`数字` 混排	 | 2.0.0 |
| memberIds	  		| array 	| 	是 	| 被移除群成员 Id 列表 | 2.0.0 |

##### 请求成功

```json
{
    "code": 200
}
```

### Group.dismiss(params){#dismiss}

解散群组

`params` 参数说明:

| 参数   	 		|	类型		| 必填	| 说明 							|最低版本	|
| :----------------	|:--------	|:-----	|:------------------------------|:----- |
| id		  		| string 	| 	是 	| 群组 Id，最大长度 30 个字符，建议使用 `英文字母`、`数字` 混排	 | 2.0.0 |
| operator	  		| string 	| 	是 	| 解散群组操作者 | 2.0.0 |

##### 请求成功

```json
{
    "code": 200
}
```

### Group.refresh(group){#refresh}

解散群组

`group` 参数说明:

| 参数   	 		|	类型		| 必填	| 说明 							|最低版本	|
| :----------------	|:--------	|:-----	|:------------------------------|:----- |
| id		  		| string 	| 	是 	| 群组 Id，最大长度 30 个字符，建议使用 `英文字母`、`数字` 混排	 | 2.0.0 |
| name		  		| string 	| 	是 	| 群组名称，最大长度 60 个字符 | 2.0.0 |

##### 请求成功

```json
{
    "code": 200
}
```

### Group.getMembers(group){#refresh}

解散群组

`group` 参数说明:

| 参数   	 		|	类型		| 必填	| 说明 							|最低版本	|
| :----------------	|:--------	|:-----	|:------------------------------|:----- |
| id		  		| string 	| 	是 	| 群组 Id，最大长度 30 个字符，建议使用 `英文字母`、`数字` 混排	 | 2.0.0 |

##### 请求成功

```json
{
	"code": 200,
	"members": [{
		"id": "小明"
	}]
}
```

| 参数   	 |	类型		| 说明	
| :----------|:--------	|:-----	
|	members	 |	array	| 成员列表

